#compdef sshfs
#autoload

autoload -Uz _ssh_hosts _sshfs

# number of args after the command name
local argc=$(( $#words - 1 ))

if (( argc == 1 )) && [[ ${words[2]} != -* ]]; then
  _ssh_hosts
  return
fi

_sshfs
