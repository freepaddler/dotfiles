# vim: ft=bash
_env_cmd bat || return 0

mkdir -p "$(bat --config-dir)"
cat >| "$(bat --config-file)" << EOF
#--theme="Nord"
--theme="Catppuccin Macchiato"
--map-syntax "$XDG_CONFIG_HOME/shell/**:sh"
EOF

alias less='bat -p'
man() {
    batman "$@"
}

_env_shell bash && . <(bat --completion bash)
